# ☁️ תכנון ארכיטקטורת ענן ושרת מבוזר — KungFu Chess (`Server_Design.md`)

> **מסמך תכנון ארכיטקטורת ענן (System Design)**  
> מיועד לתמיכה ב-**100 מיליון משתמשים רשומים** ו-**10 מיליון שחקנים פעילים בו-זמנית (10M CCU)**.

---

## 📌 1. מבט-על ודיאגרמת ארכיטקטורה

הארכיטקטורה הופכת את שרת ה-C++ הקיים משרת בודד (Single Node) לשרת ענן מבוזר, עמיד בנקודות כשל (Fault-Tolerant) ובעל יכולת גדילה (Scale-Out) מבוסס **Docker**, **Kubernetes (K3s)**, **Redis Cluster**, **PostgreSQL** ו-**NATS Message Bus**.

```text
                           [ נתב עומסים עולמי / Cloudflare CDN ]
                                          │
                                          ▼
                             [ Load Balancer מרכזי ]
                                          │
                  ┌───────────────────────┴───────────────────────┐
                  ▼                                               ▼
     [ שרתי שער - Gateways ]                         [ שרות התאמת יריבים - Matchmaking ]
   (קונטיינרי Docker ללא State)                   (קונטיינרי Docker ללא State)
                  │                                               │
                  ├───────────────────────┬───────────────────────┤
                  ▼                       ▼                       ▼
          [ Redis Cluster ]     [ PostgreSQL Cluster ]      [ NATS Message Bus ]
          (ניהול זיכרון וחדרים)    (משתמשים, ELO, היסטוריה)   (סנכרון הודעות בזמן אמת)
                  │                                               │
                  └───────────────────────┬───────────────────────┘
                                          ▼
                          [ אשכול שרתי משחק (K3s Cluster) ]
                     (קונטיינרי Docker עם State שמריצים C++)
                     ├── קונטיינר 1: חדרים 1..1000 (לולאת 30 FPS)
                     ├── קונטיינר 2: חדרים 1001..2000
                     └── קונטיינר N: ...
```

---

## 🗄️ 2. בחירת מסד נתונים: 100 מיליון משתמשים רשומים

### ❌ למה SQLite נכשל לחלוטין בהיקף כזה?
1. **נעילת קובץ מלאה (File-Level Locking):** ברגע שמשתמש אחד כותב ל-SQLite, כל הקובץ ננעל. תחת עומס של 100 מיליון משתמשים, אלפי ניסיונות התחברות ורישום בו-זמניים יגרמו לחסימה מלאה (Deadlock), השהיות קשות וקריסה.
2. **חוסר יכולת להתרחב למספר שרתים (No Sharding / Horizontal Scaling):** SQLite רץ רק על קובץ בודד במחשב אחד. אי אפשר לפצל אותו על פני 50 שרתי ענן.
3. **ללא Connection Pooling או תקשורת שרת-לקוח:** SQLite אינו מסד נתונים מבוזר עם פרוטוקול רשת, אלא מנוע מבוסס קובץ מקומי.

### ✅ הארכיטקטורה המוצעת: שילוב מסדי נתונים (Polyglot Persistence)

אנו משתמשים בשני מסדי נתונים המשלימים זה את זה:

| שכבה | טכנולוגיה | תפקיד במערכת | אסטרטגיית גדילה (Scaling / Sharding) |
|---|---|---|---|
| **מסד נתונים ראשי** | **PostgreSQL** (עם PgBouncer) | שמירת חשבונות, סיסמאות מוצפנות, היסטוריית משחקים ודירוג ELO קבוע. | פיצול אופקי (Horizontal Sharding) לפי Hash של `user_id`. שרת כתיבה ראשי (Primary) + 5 שרתי קריאה (Read Replicas). |
| **מסד נתונים בזיכרון (Cache)** | **Redis Cluster** | ניהול Tokens של Session, סטטוס אונליין, מיקום חדרים פעילים ולוח מובילים (Leaderboard). | Redis Cluster מבוזר עם 16,384 Hash Slots המפוזרים על 12 שרתים (6 Masters + 6 Replicas). |

---

## 🌐 3. תמיכה ב-10 מיליון משתמשים בו-זמנית (10M CCU)

### האם שרת אחד מספיק?
**חד משמעית לא.** שרת Linux חזק בודד מסוגל להחזיק כ-50,000 עד 100,000 חיבורי WebSocket בו-זמנית (בגלל מגבלות OS File Descriptors וזיכרון RAM לחיבור).  
כדי לתמוך ב-**10,000,000 שחקנים פעילים**, נדרש אשכול (Cluster) של **כ-200 עד 500 קונטיינרי Docker** המבוזרים באזורי ענן שונים (AWS / Cloudflare).

### איך שחקנים מוצאים זה את זה ונכנסים לכל חדר ברחבי העולם?

1. **שרתי שער (API Gateways - Docker):** קולטים את חיבור ה-WebSocket של השחקן ומבצעים אימות.
2. **שרות התאמת יריבים (Matchmaking Microservice):** מקבל בקשות `FIND_MATCH` ומצוות שחקנים בעלי ELO דומה מתוך תור מרכזי ב-Redis Sorted Set (`ZSET`).
3. **רישום מיקום החדר ב-Redis (Room Location Registry):** כשיריבים מצוותים או נוצר חדר `RM-1001`, מוקצה עבורם חדר והמיקום שלו נרשם ב-Redis:
   ```json
   "room:RM-1001": {
     "node_ip": "10.0.4.12",
     "port": 8080,
     "white_user": "shira",
     "black_user": "uria"
   }
   ```
4. **ניתוב שקוף (Transparent Proxying):** אם שחקן א' מחובר לשרת שער 1 ושחקן ב' מחובר לשרת שער 2, שרתי השער מעבירים את הודעות ה-WebSocket ישירות לקונטיינר ה-C++ שמריץ את החדר (`10.0.4.12`) דרך רשת NATS / gRPC פנימית.

---

## 📊 4. חישוב נפח תעבורת רשת (Network Bandwidth Calculations)

### נתוני יסוד
- **שחקנים פעילים:** 10,000,000 שחקנים ברגע נתון.
- **קצב מהלכים:** ממוצע של מהלך אחד כל 2 שניות לשחקן.
- **גודל בקשת מהלך (Upstream):** כ-200 Bytes (`MOVE seq sr sc dr dc`).
- **גודל שידור עדכון (Downstream):** כ-200 Bytes בפריים דלתא דחוס × 30 FPS $\approx$ 6 KB בשנייה לשחקן.

### תעבורה נכנסת מהשחקנים לשרת (Upstream)
$$\text{בקשות מהלך בשנייה} = \frac{10,000,000 \text{ players}}{2 \text{ seconds}} = 5,000,000 \text{ requests/sec}$$

$$\text{רוחב פס נכנס} = 5,000,000 \times 200 \text{ bytes} = 1,000,000,000 \text{ B/s} = 1 \text{ GB/sec} = 8 \text{ Gbps}$$

### תעבורה יוצאת מהשרת לשחקנים (Downstream)
$$\text{רוחב פס יוצא} = 10,000,000 \text{ players} \times 6 \text{ KB/sec} = 60,000,000 \text{ KB/sec} = 60 \text{ GB/sec} = 480 \text{ Gbps}$$

### ניתוח נפח התעבורה
- **האם 480 Gbps זה הרבה ברשת האינטרנט?**  
  עבור שרת יחיד זה נפח עצום ובלתי אפשרי (כרטיס רשת של שרת רגיל הוא 10Gbps או 25Gbps).  
  אבל כשהעומס מבוזר על אשכול של **200 קונטיינרים** מאחורי Cloudflare / AWS Load Balancer, כל קונטיינר מעביר כ-**2.4 Gbps בלבד** — נפח תקני לחלוטין ברשתות ענן מודרניות.

---

## ⏱️ 5. משחק קצר (30–90 שניות) ותפקידי ה-Dockerים

בגלל שמשחק נמשך בין 30 ל-90 שניות בלבד, יש קצב מטורף של יצירת וסגירת חדרים (~100,000 סיומי משחק בשנייה בעולם).

### חלוקת תפקידים לקונטיינרי ה-Docker

```text
                                  [ תפקידי קונטיינרי ה-DOCKER ]
                                              │
                   ┌──────────────────────────┴──────────────────────────┐
                   ▼                                                     ▼
     [ קונטיינרים ללא State ]                             [ קונטיינרים עם State ]
  (שרתי שער, אימות, התאמת יריבים)                       (מנועי משחק C++ ב-K3s)
  • עולים ויורדים דינמית לפי עומס                       • קונטיינרים קבועים בזיכרון
  • ללא זיכרון מקומי (Stateless)                      • מנהלים בריכה של 1,000 חדרים
  • יצירה והריסה מהירה בשניות                         • מוחקים וממחזרים חדרים בזיכרון בלבד
```

1. **קונטיינרים ללא State (Stateless - API/Auth/Matchmaker):**
   - מטפלים בחיבורי HTTP/WebSocket קצרים.
   - עולים ויורדים אוטומטית לפי עומס (Kubernetes Auto-scaling) כי הם לא שומרים זיכרון מקומי.
2. **קונטיינרים עם State (Stateful - Game Server Workers):**
   - **לא יוצרים Docker נפרד לכל חדר!** (כי להעלות קונטיינר לוקח 1-2 שניות, שזה איטי מדי למשחק של 30 שניות).
   - במקום זאת, קונטיינר C++ מחזיק **בריכת זיכרון קבועה של 1,000 חדרים**. כשהמשחק נגמר (כעבור 30s), ה-Slot של החדר ממוחזר מיד למשחק הבא ללא צורך בהעלאת קונטיינר חדש!

---

## 🐳 6. סקירת טכנולוגיות: Docker, Kubernetes ו-K3s

### 1. Docker (דוקר)
- **מה זה:** כלי שאורז את הקוד, הקבצים הבינאריים של C++, והספריות (`OpenCV`, `ASIO`, `WebSocketPP`) לתוך קופסה ניידת שנקראת **Container Image**.
- **התפקיד בפרויקט:** מבטיח ששרת המשחק (`socket_server`) ירוץ בצורה זהה לחלוטין בכל סביבה — ב-Windows המקומי שלך, בשרת Linux, או בענן AWS.

### 2. Kubernetes (K8s)
- **מה זה:** מערכת תזמור וניהול (Orchestration) המנהלת אלפי קונטיינרי Docker. היא דואגת לחלוקת עומסים, הרמת קונטיינרים חדשים כשיש עומס (Auto-scaling), והחלפת קונטיינרים שנפלו (Self-healing).
- **התפקיד בפרויקט:** מנהלת את כל אשכול השרתים (Gateways, Matchmakers ו-Game Workers).

### 3. K3s
- **מה זה:** גרסה קלה, מהירה ומותאמת של Kubernetes (פותחה ע"י Rancher) שדורשת פחות מ-100MB RAM לשרת.
- **התפקיד בפרויקט:** כלי מושלם לפיתוח, בדיקות מקומיות והרמת אשכול שרתים קל ומהיר ללא סרבול.
